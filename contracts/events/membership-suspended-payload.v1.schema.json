{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://epd2.example/contracts/events/membership-suspended-payload.v1.schema.json",
  "title": "membership.membership_suspended event payload (v1)",
  "description": "docs/canonical/TZ-00-domain-event-canon.md, section 19d.16 (canon-0.6.0, PACK-07, ADR-030). Produced by epd2_membership_service.events.build_membership_suspended_event - canon 19d.16's second consequential outcome requiring an authorized human decision reference (recorded in the corresponding MembershipDecisionRecorded/audit trail, not repeated on this minimal payload). membership_status is always 'suspended' here.",
  "type": "object",
  "additionalProperties": false,
  "required": ["membership_id", "membership_status"],
  "properties": {
    "membership_id": {
      "type": "string",
      "format": "uuid"
    },
    "membership_status": {
      "type": "string",
      "enum": ["suspended"]
    }
  }
}

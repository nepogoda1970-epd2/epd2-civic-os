{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://epd2.example/contracts/events/regional-scope-access-revoked-payload.v1.schema.json",
  "title": "regional_scope_access.revoked event payload (v1)",
  "description": "docs/canonical/TZ-00-domain-event-canon.md, section 20.5/19e.20. Produced by epd2_organization_service.events.build_regional_scope_access_revoked_event. Minimum payload only: effective_time and recorded_at - the revoked grant's own identity lives entirely on the envelope's SubjectRef (subject_type=regional_scope_access), not duplicated into the payload.",
  "type": "object",
  "additionalProperties": false,
  "required": ["effective_time", "recorded_at"],
  "properties": {
    "effective_time": { "type": "string", "format": "date-time" },
    "recorded_at": { "type": "string", "format": "date-time" }
  }
}

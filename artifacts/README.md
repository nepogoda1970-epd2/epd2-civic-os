# Temporary PILOT-04 C7 authoritative inputs

Verification-only transport directory. Do not merge this branch.

The GitHub gate is triggered only when the binary input files are uploaded here. The governed archives are reconstructed byte-for-byte and independently SHA-256 verified before the candidate validator is allowed to run.

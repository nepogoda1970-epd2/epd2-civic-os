# TypeScript Packages

Shared TypeScript packages for EPD² Civic OS. No domain types are defined
here at this stage — only infrastructure-level version constants.

## Packages

- `epd2-types` — `CANON_VERSION` and `REPOSITORY_VERSION` constants, kept
  in sync with the Python `epd2-core` package.

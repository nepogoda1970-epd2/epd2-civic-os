# SEC handoff upload slot

Transport only. Upload the exact SEC candidate/review package and its report, lineage manifest, inventory, and SHA-256 here when ready. SEC must consume the exact final integrated baseline. Independent authoritative verification uses a separate acceptance lineage.

# OPS handoff upload slot

Transport only. Upload the exact sealed OPS candidate ZIP and its developer report, inventory/manifest, and SHA-256 here when ready. Do not re-pack sealed bytes. Independent acceptance uses a separate acceptance lineage.

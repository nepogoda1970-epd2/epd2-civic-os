node_modules
.next
dist
coverage
.venv
uv.lock
package-lock.json
*.csv

# Canonical document is frozen and must not be reformatted outside an
# accepted ADR (see docs/canonical/README.md).
docs/canonical/TZ-00-domain-event-canon.md

# Deterministic package/seal metadata is generated after the source-format
# gate and is verified by exact-byte/package-seal checks, not source Prettier.
ACCEPTANCE/FREEZE-INVENTORY.json

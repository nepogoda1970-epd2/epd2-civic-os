# CI Affected Graph v1 → v1.1 — developer-CI accelerator (2026-09-05)

**Status:** v1.1 safety-correction round committed on isolated branch `feature/ci-affected-graph-v1`: commits `9ad0f318…` (v1) and `2900648113f089b9d16974beb3d21dcc33f5cf0e` (v1.1), both on `main@7544f5dc3bf40304ae81b4d8ef476cc8ecb60ec5`. **Not pushed** (session has read-only GitHub access) — delivered as git bundle + patches to the owner. Developer-CI only; no governance/PCR/FIR/entrypoint/INFRA-04/acceptance-workflow/evidence/snapshot/metadata change; nothing marked PASS/ACCEPTED/CLOSED/READY; nothing merged. Rollout: parallel/informational shadow period; existing `CI` workflow unchanged and remains the gate. If INFRA-04's corrected baseline lands first: **REBASE REQUIRED AFTER INFRA-04** (no baseline fixes are in this branch).

## v1.1 corrections (all tested; 127 planner tests green; mypy/ruff/prettier clean)

- Git topology: `merge-base`, or proven GitHub PR merge-commit topology (`github-pr-merge-parent`: HEAD two parents; first == event base.sha == resolved base; second == event head.sha; parents via `git cat-file -p`), else `GIT_RANGE_UNPROVEN` → full. Two-tree diffs never used. PR base ≠ `main` → `GIT_BASE_UNVALIDATED` → full. 15 adversarial topology tests assert planned == full AND coverage ⊇ Makefile/ci.yml baseline.
- Fail-safe invariant machine-tested over 12 uncertainty conditions; `coverage(scoped) ⊆ coverage(full)`; `fallback_full ⇒ steps == full_steps` by construction.
- External refs: top-level directory token scan (bare token → whole dir; literal prefix → prefix); import scan; documented A/B/C path classes. Undeclared edges preserved (membership→eligibility/governance/identity; eligibility→credential/governance; voting-client & representative-workspace→web-shell). Effective SCC = {eligibility, governance, voting} with edge witness.
- Samples/graph summary generated by `scripts/ci/generate_samples.py`; `--check` in test suite; report excerpt and rule-id list are consistency-tested.
- Executor mutation detector: step that modifies tracked files → `mutated` → lane fails; never restores. Workflow: Python 3.12 in every job, Node 22, frozen installs, event SHAs + base ref passed, outputs via env, lane backstop, artifact overwrite.
- Shadow validation: `shadow_comparison` block in manifest; `scripts/ci/shadow_compare.py` (exit 2 = planner defect); ci.yml→step-id mapping rule in report §9.

## Known open items (documented in report §6/§7/§10)

Pre-existing red developer baseline on main (ruff, mypy scripts/, tests/repository collection, voting-service hypothesis test, prettier 251 files, web-shell capture spec + voting timings rewrite tracked files → now fail the lane by design). Residual false-negative channels: runtime-computed paths without literal tokens, literal container-prefixed sibling reads (`"services/x/…"`, none at baseline), per-process pytest `sys.path` collisions, cross-ecosystem effects. Metadata cleanup of undeclared edges is a later governed task.

## Owner action

`git fetch <bundle> feature/ci-affected-graph-v1:feature/ci-affected-graph-v1 && git push -u origin feature/ci-affected-graph-v1`

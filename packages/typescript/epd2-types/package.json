{
  "name": "epd2-types",
  "version": "0.1.0",
  "description": "EPD2 Civic OS shared TypeScript infrastructure package (no domain types)",
  "private": true,
  "license": "SEE LICENSE IN ../../../LICENSE",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "tsx": "^4.15.0",
    "prettier": "^3.3.0"
  }
}

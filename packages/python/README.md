# Python Packages

Shared Python packages for EPD² Civic OS. These packages contain no
business logic — only infrastructure-level types and utilities shared
across future services.

## Packages

- `epd2-core` — canon/repository version constants and UUID identifier
  helpers.
